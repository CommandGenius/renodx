#include "./common.hlsl"

float4 c0 : register(c0);
float4 c1 : register(c1);
float4 c2 : register(c2);
float4 c3 : register(c3);
float4 c4 : register(c4);
float4 c5 : register(c5);
float4 c6 : register(c6);
float4 c7 : register(c7);
float4 c8 : register(c8);
float4 c9 : register(c9);
float4 c13 : register(c13);
float4 c14 : register(c14);

sampler2D s0 : register(s0);
sampler2D s1 : register(s1);

float4 main(float4 v0 : TEXCOORD0, float4 v1 : TEXCOORD1, float2 vpos : VPOS) : COLOR0 {
  static const float4 c10 = float4(1, -1, 4, 2);
  static const float4 c11 = float4(1e+009, 0, 1, 1.44269502);
  static const float4 c12 = float4(0.949999988, 0, 0, 0);
  float4 vPos = float4(vpos, 0.f, 0.f);
  float4 r0 = 0.f, r1 = 0.f, r2 = 0.f;
  float4 o0 = 0.f;
  r0.x = (c0.xxxx).x;
  r0.y = (c1.xxxx).y;
  r0.z = (c2.xxxx).z;
  r0.w = (c3.xxxx).w;
  r1.xy = (vPos.xyzw * c13.xyzw + c13.zwzw).xy;
  r1.xyzw = (tex2D(s1, (r1.xyzw).xy)).xyzw;
  r1.z = (abs(r1.xxxx)).z;
  r2.xy = (vPos.xyzw * c14.xyzw + c14.zwzw).xy;
  r1.xy = (r1.zzzz * r2.xyzw).xy;
  r1.w = (c10.xxxx).w;
  r0.x = (dot(r1.xyzw, r0.xyzw));
  r2.x = (c0.yyyy).x;
  r2.y = (c1.yyyy).y;
  r2.z = (c2.yyyy).z;
  r2.w = (c3.yyyy).w;
  r0.y = (dot(r1.xyzw, r2.xyzw));
  r2.x = (c0.zzzz).x;
  r2.y = (c1.zzzz).y;
  r2.z = (c2.zzzz).z;
  r2.w = (c3.zzzz).w;
  r0.z = (dot(r1.xyzw, r2.xyzw));
  r1.xyz = (r0.xyzw + (-c4.xyzw)).xyz;
  r0.w = (dot((r1.xyzw).xyz, (r1.xyzw).xyz));
  r1.xyz = ((-c4.xyzw) + v0.xyzw).xyz;
  r1.x = (dot((r1.xyzw).xyz, (r1.xyzw).xyz));
  r0.w = ((-r0.wwww) + r1.xxxx).w;
  r0.xyz = (((r0.wwww) >= 0.f ? r0.xyzw : v0.xyzw)).xyz;
  r1.xyz = (r0.xyzw + (-c4.xyzw)).xyz;
  r0.w = (dot((r1.xyzw).xyz, (r1.xyzw).xyz));
  r1.x = (dot((v0.xyzw).xyz, (v0.xyzw).xyz));
  r1.x = (r1.xxxx + c10.yyyy).x;
  r1.yzw = (c4.xxyz + (-v0.xxyz)).yzw;
  r2.x = (dot((r1.yzww).xyz, (r1.yzww).xyz));
  r1.y = (dot((r1.yzww).xyz, (v0.xyzw).xyz));
  r1.x = (r1.xxxx * r2.xxxx).x;
  r1.z = (r2.xxxx + r2.xxxx).z;
  r1.x = (r1.xxxx * c10.zzzz).x;
  r1.w = (r1.yyyy + r1.yyyy).w;
  r1.x = (r1.wwww * r1.wwww + (-r1.xxxx)).x;
  r1.x = (rsqrt(abs(r1.xxxx))).x;
  r1.x = (1.f / r1.xxxx).x;
  r1.w = (r1.yyyy * (-c10.wwww) + r1.xxxx).w;
  r1.x = (r1.yyyy * (-c10.wwww) + (-r1.xxxx)).x;
  r1.y = (1.f / r1.zzzz).y;
  r1.xz = (r1.xyww * r1.yyyy).xz;
  r1.xy = ((((-r1.xzzw)) >= 0.f ? c11.xxxx : r1.xzzw)).xy;
  r2.x = saturate(min(r1.xxxx, r1.yyyy)).x;
  r1.xyz = (lerp(v0.xyzw, c4.xyzw, r2.xxxx)).xyz;
  r2.xyz = (r1.xyzw + (-c4.xyzw)).xyz;
  r0.xyz = (r0.xyzw + (-r1.xyzw)).xyz;
  r1.x = (dot((r2.xyzw).xyz, (r2.xyzw).xyz));
  r0.w = ((-r0.wwww) + r1.xxxx).w;
  r0.w = (((r0.wwww) >= 0.f ? c11.yyyy : c11.zzzz)).w;
  r1.xyz = (r0.wwww * c6.xyzw).xyz;
  r0.w = ((-c5.wwww) + v0.wwww).w;
  r1.w = (1.f / (-c9.yyyy)).w;
  r0.w = saturate(r0.wwww * r1.wwww).w;
  r1.xyz = (r1.xyzw * r0.wwww).xyz;
  r0.xyz = (r0.xyzw * c5.xyzw).xyz;
  r0.x = (dot((r0.xyzw).xyz, (r0.xyzw).xyz));
  r0.x = ((-r0.xxxx) * c8.xxxx).x;
  r0.x = (r0.xxxx * c11.wwww).x;
  r0.x = (exp(r0.xxxx)).x;
  r0.x = ((-r0.xxxx) + c10.xxxx).x;
  r0.xyz = (r1.xyzw * r0.xxxx).xyz;
  r1.xyzw = (tex2D(s0, (v1.zwzw).xy)).xyzw;
  r0.w = (r1.xxxx * c7.yyyy).w;
  r1.xyzw = (tex2D(s0, (v1.xyzw).xy)).xyzw;
  r0.w = (r1.xxxx * c7.xxxx + r0.wwww).w;
  r0.w = (r0.wwww * c7.zzzz + c7.wwww).w;
  r0.xyz = (r0.xyzw * r0.wwww + c10.xxxx).xyz;
  r1.x = (1.f / r0.xxxx).x;
  r1.y = (1.f / r0.yyyy).y;
  r1.z = (1.f / r0.zzzz).z;
  o0.xyz = (r1.xyzw * (-c12.xxxx) + c12.xxxx).xyz;
  o0.w = (c11.yyyy).w;

  o0.xyz = saturate(o0.xyz);
  o0.w += InjectionGuard();
  return o0;
}
