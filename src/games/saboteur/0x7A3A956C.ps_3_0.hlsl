#include "./common.hlsl"

float4 c0 : register(c0);
float4 c1 : register(c1);
float4 c2 : register(c2);
float4 c3 : register(c3);
float4 c4 : register(c4);
float4 c5 : register(c5);
float4 c6 : register(c6);
float4 c7 : register(c7);
float4 c8 : register(c8);
float4 c9 : register(c9);
float4 c10 : register(c10);
float4 c11 : register(c11);
float4 c12 : register(c12);
float4 c13 : register(c13);
float4 c14 : register(c14);

sampler2D s0 : register(s0);
sampler2D s1 : register(s1);

float4 main(float4 v0 : TEXCOORD0, float4 v1 : TEXCOORD1, float2 vpos : VPOS) : COLOR0 {
  static const float4 c15 = float4(1, -1, 0, 1.00000001e-007);
  static const float4 c16 = float4(1e+009, 0.5, 1.44269502, 0.949999988);
  float4 vPos = float4(vpos, 0.f, 0.f);
  float4 r0 = 0.f, r1 = 0.f, r2 = 0.f, r3 = 0.f;
  float4 o0 = 0.f;
  r0.x = (dot((c15.xyzw).xy, (v0.xzzw).xy) + (c15.zzzz).x);
  r0.yzw = ((-c4.xxyz) + v0.xxyz).yzw;
  r1.x = (dot((r0.ywzw).xy, (c15.xyzw).xy) + (c15.zzzz).x);
  r1.x = (1.f / r1.xxxx).x;
  r1.y = ((-r0.xxxx) * r1.xxxx).y;
  r0.x = ((-r0.xxxx) * (-r1.xxxx) + c15.wwww).x;
  r0.x = (((r0.xxxx) >= 0.f ? c16.xxxx : r1.yyyy)).x;
  r1.x = (dot((c15.yyyy).xy, (v0.xzzw).xy) + (c15.zzzz).x);
  r1.y = (dot((r0.ywzw).xy, (c15.yyyy).xy) + (c15.zzzz).x);
  r1.y = (1.f / r1.yyyy).y;
  r1.z = ((-r1.xxxx) * r1.yyyy).z;
  r1.x = ((-r1.xxxx) * (-r1.yyyy) + c15.wwww).x;
  r1.x = (((r1.xxxx) >= 0.f ? c16.xxxx : r1.zzzz)).x;
  r2.x = (min(r1.xxxx, r0.xxxx)).x;
  r0.x = (dot((c15.xyzw).xy, (v0.yzzw).xy) + (c15.zzzz).x);
  r1.x = (dot((r0.zwzw).xy, (c15.xyzw).xy) + (c15.zzzz).x);
  r1.x = (1.f / r1.xxxx).x;
  r1.y = ((-r0.xxxx) * r1.xxxx).y;
  r0.x = ((-r0.xxxx) * (-r1.xxxx) + c15.wwww).x;
  r0.x = (((r0.xxxx) >= 0.f ? c16.xxxx : r1.yyyy)).x;
  r1.x = (min(r0.xxxx, r2.xxxx)).x;
  r0.x = (dot((r0.zwzw).xy, (c15.yyyy).xy) + (c15.zzzz).x);
  r0.x = (1.f / r0.xxxx).x;
  r1.y = (dot((c15.yyyy).xy, (v0.yzzw).xy) + (c15.zzzz).x);
  r1.z = (r0.xxxx * (-r1.yyyy)).z;
  r0.x = ((-r1.yyyy) * (-r0.xxxx) + c15.wwww).x;
  r0.x = (((r0.xxxx) >= 0.f ? c16.xxxx : r1.zzzz)).x;
  r2.x = (min(r0.xxxx, r1.xxxx)).x;
  r0.x = (c8.yyyy + (-v0.zzzz)).x;
  r1.x = (1.f / r0.wwww).x;
  r1.y = (r0.xxxx * r1.xxxx).y;
  r0.x = (r0.xxxx * (-r1.xxxx) + c15.wwww).x;
  r0.x = (((r0.xxxx) >= 0.f ? c16.xxxx : r1.yyyy)).x;
  r1.y = (min(r0.xxxx, r2.xxxx)).y;
  r0.x = (c15.xxxx + (-v0.zzzz)).x;
  r1.z = (r1.xxxx * r0.xxxx).z;
  r0.x = (r0.xxxx * (-r1.xxxx) + c15.wwww).x;
  r0.x = (((r0.xxxx) >= 0.f ? c16.xxxx : r1.zzzz)).x;
  r2.x = (min(r0.xxxx, r1.yyyy)).x;
  r0.xyz = (r0.yzww * r2.xxxx + v0.xyzw).xyz;
  r1.xyz = (r0.xyzw + (-c4.xyzw)).xyz;
  r0.w = (dot((r1.xyzw).xyz, (r1.xyzw).xyz));
  r1.x = (c0.xxxx).x;
  r1.y = (c1.xxxx).y;
  r1.z = (c2.xxxx).z;
  r1.w = (c3.xxxx).w;
  r2.xy = (vPos.xyzw * c13.xyzw + c13.zwzw).xy;
  r2.xyzw = (tex2D(s1, (r2.xyzw).xy)).xyzw;
  r2.z = (abs(r2.xxxx)).z;
  r3.xy = (vPos.xyzw * c14.xyzw + c14.zwzw).xy;
  r2.xy = (r2.zzzz * r3.xyzw).xy;
  r2.w = (c15.xxxx).w;
  r1.x = (dot(r2.xyzw, r1.xyzw));
  r3.x = (c0.yyyy).x;
  r3.y = (c1.yyyy).y;
  r3.z = (c2.yyyy).z;
  r3.w = (c3.yyyy).w;
  r1.y = (dot(r2.xyzw, r3.xyzw));
  r3.x = (c0.zzzz).x;
  r3.y = (c1.zzzz).y;
  r3.z = (c2.zzzz).z;
  r3.w = (c3.zzzz).w;
  r1.z = (dot(r2.xyzw, r3.xyzw));
  r2.xyz = (r1.xyzw + (-c4.xyzw)).xyz;
  r1.w = (dot((r2.xyzw).xyz, (r2.xyzw).xyz));
  r0.w = (r0.wwww + (-r1.wwww)).w;
  r0.xyz = (((r0.wwww) >= 0.f ? r1.xyzw : r0.xyzw)).xyz;
  r0.w = (r0.zzzz * c8.zzzz + c8.wwww).w;
  r1.x = (v0.zzzz * c8.zzzz + c8.wwww).x;
  r2.x = (lerp(r1.xxxx, r0.wwww, c16.yyyy)).x;
  r0.z = (r2.xxxx * c8.xxxx).z;
  r1.yz = (v0.xxyw).yz;
  r0.xyw = (r0.xyzw + (-r1.yzzx)).xyw;
  r1.w = ((-r1.xxxx) + c15.xxxx).w;
  r0.xyw = (r0.xyzw * c5.xyzz).xyw;
  r0.x = (dot((r0.xyww).xyz, (r0.xyww).xyz));
  r0.x = (r0.zzzz * (-r0.xxxx)).x;
  r0.x = (r0.xxxx * c16.zzzz).x;
  r0.x = (exp(r0.xxxx)).x;
  r0.x = ((-r0.xxxx) + c15.xxxx).x;
  r0.y = (dot((r1.yzww).xyz, (c10.xyzw).xyz));
  r0.w = (dot((r1.yzww).xyz, (c11.xyzw).xyz));
  r0.z = (abs(r0.yyyy)).z;
  r0.yz = (r0.xzww * c12.xxyw + c12.xzww).yz;
  r0.y = (1.f / r0.yyyy).y;
  r0.z = (1.f / r0.zzzz).z;
  r0.y = (r0.yyyy * r0.zzzz).y;
  r0.x = (r0.xxxx * r0.yyyy).x;
  r0.y = ((-c5.wwww) + v0.wwww).y;
  r0.z = (1.f / (-c9.yyyy)).z;
  r0.y = saturate(r0.yyyy * r0.zzzz).y;
  r0.yzw = (r0.yyyy * c6.xxyz).yzw;
  r0.xyz = (r0.xxxx * r0.yzww).xyz;
  r1.xyzw = (tex2D(s0, (v1.zwzw).xy)).xyzw;
  r0.w = (r1.xxxx * c7.yyyy).w;
  r1.xyzw = (tex2D(s0, (v1.xyzw).xy)).xyzw;
  r0.w = (r1.xxxx * c7.xxxx + r0.wwww).w;
  r0.w = (r0.wwww * c7.zzzz + c7.wwww).w;
  r0.xyz = (r0.xyzw * r0.wwww + c15.xxxx).xyz;
  r1.x = (1.f / r0.xxxx).x;
  r1.y = (1.f / r0.yyyy).y;
  r1.z = (1.f / r0.zzzz).z;
  o0.xyz = (r1.xyzw * (-c16.wwww) + c16.wwww).xyz;
  o0.w = (c15.zzzz).w;

  o0.xyz = saturate(o0.xyz);
  o0.w += InjectionGuard();
  return o0;
}
